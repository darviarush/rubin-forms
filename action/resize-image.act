
$path = $request->param("path");

$root = $app->model->img(undef)->root;

return $response->not_found unless $path =~ m!^$root/((?:[^/]/)+)([^/]+)$!;

$id = $1;
$name = $2;
$ext = $3;

$id =~ s!/!!g;

$id = Utils::from_radix($id, 62);

$app->model->img($id)->resize($name);

$response->redirect($path);
