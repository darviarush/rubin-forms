# Выход пользователя

$response->cookie("sess", "", expires => 150723948, path => "/"),
$request->session->erase, $request->cookie->{sess} = undef if defined $request->cookie("sess");

::msg($app->json->encode({'logout'=>1, stash=>$app->{stash} }));

return $response->redirect;